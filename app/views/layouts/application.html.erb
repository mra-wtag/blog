<!DOCTYPE html>
<html>
<head>
  <title><%= t 'title' %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div id="top-nav-bar">
  <% if signed_in? %>
    <div id="nav-bar-info" class="row">
      <div id="email"><%= current_user.email %></div>
      <%= link_to t('nav_bar.sign_out'), sign_out_path, method: :delete, id: "sign-out", class: 'btn btn-primary' %>
    </div>
  <% end %>
</div>

<div id="main-container" class="container">
  <div class="panel panel-default">
    <div class="panel-heading"><%= t 'title' %>...</div>
    <div class="panel-body">
      <div class="row">
        <div class="col-xs-12">
          <%= yield %>
        </div>
      </div>
    </div>
    <div class="panel-footer">@sahedcse.se@gmail.com</div>
  </div>
</div>

</body>
</html>
